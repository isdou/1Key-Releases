## 1Key 1.1.1

- 换用重新绘制的黑银色 1K 应用图标和适配系统外观的菜单栏图标。
- 精简菜单栏下拉顶部，移除账号数量和重复的更新时间；底部显示自动刷新状态，悬停查看定时间隔与刷新条件。
- 加入可更新的模型价格表，补充 GPT-6 Astra、Grok 4.6 等模型的 API 等价费用估算。
- 支持离线价格副本与本地缓存，扫描本机用量时最多每小时检查一次价格更新。
- 修复未定价模型被显示为 $0.00：Token 保持完整统计，费用显示“暂无估价”或标注“部分”。
- 重新计算本机历史记录，让概览、详情页和菜单栏使用一致的估价结果。

**安装要求：** macOS 14 或更高版本，支持 Apple 芯片与 Intel。DMG 使用 Developer ID 签名并经过 Apple 公证。

费用为 API 等价价格估算，不是订阅账单。当前不会从会话元数据自动识别快速模式或长上下文附加费。
